// Placeholder for MessageNode definition
export const MessageNode = {
    type: 'MessageNode',
    label: 'Message',
    ports: { in: [{ id: 'in', label: 'In' }], out: [{ id: 'out', label: 'Out' }] },
    data: { messageText: '' }
};
